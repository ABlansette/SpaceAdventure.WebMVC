@model IEnumerable<SpaceAdventure.Models.Adventurer.AdventurerListItems>


@{
    ViewBag.Title = "List";
}

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AdventurerId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Level)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Class)
        </th>
        <th></th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AdventurerId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Level)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Class)
            </td>
            <td>
                @Html.ActionLink("Edit Name", $"Edit{item.AdventurerId}", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("View Adventurer", $"Details{item.AdventurerId}", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", $"Delete{item.AdventurerId}", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
    }

</table>
@Html.ActionLink("Settings", "../Home/Settings")
